## Test plan

- [ ] `make test` passes
- [ ] `make lint` passes (or no new lint issues)
- [ ] E2e passes if CLI behavior changed: `make test-e2e`
- [ ] `make sync-upstream` (or overlay edit) if upstream CLI flags / SKILL changed
